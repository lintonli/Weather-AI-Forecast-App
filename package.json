{
  "name": "weather-ai-dashboard",
  "version": "1.0.0",
  "private": true,
  "description": "A clean weather dashboard powered by the WeatherAI API (weather-ai.co) with AI-generated summaries.",
  "scripts": {
    "dev": "npx concurrently -n backend,frontend -c blue,magenta \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "build": "npm run build --prefix frontend && npm run build --prefix backend",
    "start": "npm run start --prefix backend"
  },
  "license": "MIT"
}
